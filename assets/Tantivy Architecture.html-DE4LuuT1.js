import{_ as n,c as s,a,o as i}from"./app-DfFNXaa-.js";const t={};function l(o,e){return i(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="tantivy-architecture" tabindex="-1"><a class="header-anchor" href="#tantivy-architecture"><span>Tantivy - Architecture</span></a></h1><p>Created by: Mr Dk.</p><p>2026 / 05 / 25 16:00</p><p>Hangzhou, Zhejiang, China</p><hr><h2 id="背景" tabindex="-1"><a class="header-anchor" href="#背景"><span>背景</span></a></h2><p>Tantivy 是一个用 Rust 实现的全文搜索引擎库。它的架构深受 Apache Lucene 影响——如果你熟悉 Lucene 的概念体系，会发现两者在术语和设计模式上有大量重叠。</p><p>全文搜索要解决的核心问题是：给定一组文本文档和一个查询，快速返回最相关的 K 篇文档。为了高效地执行这个检索，需要预先构建索引。Tantivy 默认使用 BM25 作为相关性评分算法，这和 Lucene / Elasticsearch 的默认行为一致。</p><p>Tantivy 的设计遵循几个原则：</p><ul><li>搜索应当是 O(1) 内存的——不管索引多大，搜索时的内存占用恒定</li><li>索引应当是亚线性内存的——通过定期 flush 控制内存上界</li><li>搜索应当尽可能快</li><li>索引更新以批量方式进行，不支持单文档实时更新</li></ul><p>这些取舍使得 Tantivy 非常适合构建离线索引、批量写入的搜索场景。本文围绕 segment 这一核心抽象，介绍 Tantivy 的整体架构，以及索引和搜索两条主线的数据流。</p><h2 id="segment-核心抽象" tabindex="-1"><a class="header-anchor" href="#segment-核心抽象"><span>Segment：核心抽象</span></a></h2><p>和 Lucene 一样，Tantivy 的 Index 并不是一个单一的整体数据结构，而是由若干个 <strong>segment</strong> 组成的集合。每个 segment 是一个独立、不可变的数据单元，包含一批文档的完整索引信息。这种设计使得新文档的写入只需要追加新的 segment 而无需修改已有数据，天然支持并发读写。</p><p>一个 segment 由 UUID 标识。在磁盘上，每个 segment 由 7 个文件组成，分别承载不同的数据结构。索引目录中还有一个 <code>meta.json</code> 文件记录当前有哪些 segment、每个 segment 包含多少文档，以及索引的 schema 定义。每次 commit 时 <code>meta.json</code> 被原子更新，这是索引一致性的保障。</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">index_directory/</span>
<span class="line">├── meta.json                         # segment 列表 + schema 定义</span>
<span class="line">│</span>
<span class="line">├── &lt;uuid&gt;.term                       # Term Dictionary (词典)</span>
<span class="line">├── &lt;uuid&gt;.idx                        # Postings (倒排列表)</span>
<span class="line">├── &lt;uuid&gt;.pos                        # Positions (词项位置)</span>
<span class="line">├── &lt;uuid&gt;.fast                       # Fast Fields (列式存储)</span>
<span class="line">├── &lt;uuid&gt;.fieldnorm                  # Field Norms (字段长度)</span>
<span class="line">├── &lt;uuid&gt;.store                      # Store (文档原文)</span>
<span class="line">└── &lt;uuid&gt;.&lt;opstamp&gt;.del              # Alive Bitset (存活位图)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在每个 segment 内部，文档由一个从 0 开始的紧凑整数 <code>DocId</code>（<code>u32</code>）标识。这个紧凑 ID 空间是后续所有数据结构能够高效压缩的关键前提——posting list 的 delta 编码、fast field 的定长数组访问，都依赖于 DocId 从 0 到 N-1 连续分布这一事实。</p><p>删除操作不修改 segment 文件（segment 是不可变的），而是在 alive bitset 中将对应 DocId 标记为&quot;已死亡&quot;。被删除的文档在搜索时会被过滤掉，直到后续的 merge 操作将其物理清除。</p><h2 id="从一篇文档看索引存储" tabindex="-1"><a class="header-anchor" href="#从一篇文档看索引存储"><span>从一篇文档看索引存储</span></a></h2><h3 id="评分模型-tf-idf-bm25" tabindex="-1"><a class="header-anchor" href="#评分模型-tf-idf-bm25"><span>评分模型：TF / IDF / BM25</span></a></h3><p>在深入各个文件之前先简单介绍一下评分模型——后面看到的几个数据结构都是为这个评分函数服务的。常用的评分模型有三个核心概念：</p><ul><li><strong>TF（Term Frequency，词频）</strong>：某个 term 在一篇文档中出现的次数。</li><li><strong>IDF（Inverse Document Frequency，逆文档频率）</strong>：衡量一个 term 的&quot;信息量&quot;——常见词信息量低，罕见词信息量高。</li><li><strong>BM25</strong>：在 TF 和 IDF 之上加入文档长度归一化得到最终评分，是 Tantivy 默认采用的算法。</li></ul><p>下面依次给出每个模型的公式。</p><p><strong>TF</strong> 直接采用词频计数，直觉是出现次数越多越相关，但这种相关性不是线性的——出现 100 次未必比出现 10 次更相关，因此 BM25 会再对其做一层饱和归一化（详见下文）。</p><p><strong>IDF</strong> 的公式如下：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>其中 <code>N</code> 是文档总数，<code>df(t)</code> 是包含 term <code>t</code> 的文档数。&quot;the&quot; 这种几乎在所有文档里都出现的词 IDF 接近 0，而 &quot;tantivy&quot; 这种只在少数文档里出现的词 IDF 很高。</p><p><strong>BM25</strong> 对于查询 <code>Q</code> 和文档 <code>D</code>，分数为各 term 贡献之和：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">                              TF(t,D) * (k1 + 1)</span>
<span class="line">BM25(Q,D) = Σ IDF(t) * ──────────────────────────────────</span>
<span class="line">              t∈Q       TF(t,D) + k1 * (1 - b + b * |D|/avgdl)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>k1</code>（默认 1.2）控制 TF 的饱和速度，<code>b</code>（默认 0.75）控制长度归一化的强度；<code>|D|</code> 是当前文档长度，<code>avgdl</code> 是平均文档长度。直觉上 BM25 在说两件事：</p><ol><li>相同 TF 下短文档比长文档更相关。</li><li>TF 的边际收益随次数递减。</li></ol><p>从公式可以反推索引必须存什么：</p><ul><li><code>TF(t,D)</code> → 每个 <code>(term, doc)</code> 对的词频 → 存在 postings 里</li><li><code>df(t)</code> → 每个 term 出现在多少篇文档中 → 存在 term dictionary 的 <code>TermInfo</code> 里</li><li><code>|D|</code> → 每篇文档每个字段的长度 → 存在 field norms 里</li><li><code>avgdl</code> → 全局统计，从所有 field norms 累加得到</li></ul><p>这正是 <code>.idx</code>、<code>.term</code>、<code>.fieldnorm</code> 三个文件各自承载的核心数据。</p><h3 id="字段选项" tabindex="-1"><a class="header-anchor" href="#字段选项"><span>字段选项</span></a></h3><p>Tantivy 在定义 schema 时，可以为每个字段打上若干选项标志，告诉索引该字段需要哪些用途。常用的标志有：</p><ul><li><code>TEXT</code>：建倒排索引，字段值经过分词器拆分为 tokens 并记录位置信息，支持全文检索（包括短语查询）</li><li><code>STRING</code>：建倒排索引，但整个字段值作为单个 token 不分词，适用于 ID、状态等需要精确匹配的关键字字段</li><li><code>STORED</code>：在 <code>.store</code> 文件中逐字保留字段的原始值，搜索完成后可取回展示</li><li><code>FAST</code>：在 <code>.fast</code> 文件中建列式存储，支持按 <code>DocId</code> 随机访问，用于排序、聚合、过滤</li></ul><p>这些标志互相独立，可以用 <code>|</code> 组合。例如 <code>TEXT | STORED</code> 既能搜索又能取回原文；只标 <code>FAST</code> 而不标 <code>STORED</code> 则不在 <code>.store</code> 里保留原值，但仍可从列存里读出。</p><p>把&quot;索引&quot;和&quot;保存原文&quot;拆成两件事是有意为之：倒排索引只保留分词后的 token，且经过小写化等处理。例如 &quot;The Old Man and the Sea&quot; 进倒排索引后变成 <code>[the, old, man, and, the, sea]</code>，大小写、空格、顺序全部丢失——这些信息够用来检索，但不够展示给用户；<code>STORED</code> 字段则把原文逐字保留下来。</p><h3 id="各文件承载的数据" tabindex="-1"><a class="header-anchor" href="#各文件承载的数据"><span>各文件承载的数据</span></a></h3><p>假设我们的 schema 定义了两个字段：<code>title</code>（<code>TEXT | STORED</code>）和 <code>price</code>（<code>FAST</code>）。现在索引一篇文档：</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token punctuation">{</span> <span class="token property">&quot;title&quot;</span><span class="token operator">:</span> <span class="token string">&quot;The Old Man and the Sea&quot;</span><span class="token punctuation">,</span> <span class="token property">&quot;price&quot;</span><span class="token operator">:</span> <span class="token number">42</span> <span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>这篇文档被分配了 <code>DocId = 3</code>（假设 segment 中已有 3 篇文档）。分词器将 title 拆分为 tokens：<code>[the, old, man, and, the, sea]</code>，位置分别为 <code>[0, 1, 2, 3, 4, 5]</code>。接下来各数据结构分别存储以下内容：</p><p><strong>Term Dictionary（<code>.term</code>）</strong>：存储 term 到元数据的映射。<code>TermInfo</code> 中记录了三项信息：</p><ul><li><code>doc_freq</code>：包含该 term 的文档数，用于计算 IDF。将其直接存在这里，可以在计算 IDF 时无需加载整个 posting list</li><li><code>postings_range</code>：posting list 在 <code>.idx</code> 文件中的字节范围</li><li><code>positions_range</code>：位置信息在 <code>.pos</code> 文件中的字节范围</li></ul><p>对于这篇文档涉及的 term：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">&quot;and&quot; ──▶ TermInfo { doc_freq, postings_range, positions_range }</span>
<span class="line">&quot;man&quot; ──▶ TermInfo { doc_freq, postings_range, positions_range }</span>
<span class="line">&quot;old&quot; ──▶ TermInfo { doc_freq, postings_range, positions_range }</span>
<span class="line">&quot;sea&quot; ──▶ TermInfo { doc_freq, postings_range, positions_range }</span>
<span class="line">&quot;the&quot; ──▶ TermInfo { doc_freq, postings_range, positions_range }</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Postings（<code>.idx</code>）</strong>：对于每个 term，存储包含该 term 的文档 ID 列表及词频。<code>DocId</code> 列表按升序排列，每 128 个为一个 block，block 内使用 delta 编码 + bitpacking 压缩（所有值用相同位宽）；不足 128 个的尾部用 VInt 编码。词频与 <code>DocId</code> 分开编码，同样以 128 为一个 block 进行 bitpacking。</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">&quot;the&quot; ──▶ [(DocId=0, tf=1), (DocId=1, tf=3), (DocId=3, tf=2)]</span>
<span class="line">&quot;sea&quot; ──▶ [(DocId=3, tf=1)]</span>
<span class="line">&quot;old&quot; ──▶ [(DocId=2, tf=1), (DocId=3, tf=1)]</span>
<span class="line">...</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Positions（<code>.pos</code>）</strong>：记录 term 在每篇文档中出现的具体位置，使用与 postings 相同的 delta 编码 + bitpacking 方案（128 个位置为一个 block）。位置信息使短语查询成为可能——搜索 &quot;old man&quot; 时，不仅要求两个 term 都出现在同一篇文档中，还要求 &quot;old&quot; 的位置恰好在 &quot;man&quot; 之前一位。</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">&quot;the&quot; in DocId=3 ──▶ positions: [0, 4]</span>
<span class="line">&quot;sea&quot; in DocId=3 ──▶ positions: [5]</span>
<span class="line">&quot;old&quot; in DocId=3 ──▶ positions: [1]</span>
<span class="line">...</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Fast Fields（<code>.fast</code>）</strong>：列式存储，按 <code>DocId</code> 随机访问。Tantivy 会根据数据特征自动选择最优编码（bitpacked、linear、blockwise-linear 等）；由于 <code>DocId</code> 空间连续，访问任意一个值都是 O(1) 的。下面是 <code>price</code> 字段的存储，<code>price[3] = 42</code> 对应我们刚索引的这篇文档：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">price[0] = 35</span>
<span class="line">price[1] = 28</span>
<span class="line">price[2] = 99</span>
<span class="line">price[3] = 42</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Field Norms（<code>.fieldnorm</code>）</strong>：每篇文档的每个 <code>TEXT</code> 字段存储一个 token 计数（用于 BM25 评分），每个值通过对数查找表编码为 1 字节（小值无损，大值有精度损失，与 Lucene 方案相同）。BM25 用它来惩罚过长的文档——同样命中了搜索词，短文档的相关性通常高于长文档。下面 <code>title_norm[3] = 6</code> 对应 <code>TEXT</code> 字段 <code>title</code> 中 &quot;The Old Man and the Sea&quot; 的 6 个 token：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">title_norm[0] = 5</span>
<span class="line">title_norm[1] = 12</span>
<span class="line">title_norm[2] = 3</span>
<span class="line">title_norm[3] = 6</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Store（<code>.store</code>）</strong>：行式压缩存储，保存标记为 <code>STORED</code> 的字段原始值。多篇文档打包为一个 block（默认 16KB），整块使用 LZ4 或 Zstd 压缩；读取单篇文档需要先定位 block，再整块解压。因此 store 只用于搜索完成后取回 top-K 结果的展示，不参与评分过程。</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">DocId=3 ──▶ { &quot;title&quot;: &quot;The Old Man and the Sea&quot; }</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>可以看到，同一篇文档的数据被&quot;打散&quot;存储到了不同的文件中。这不是冗余，而是各数据结构各司其职：term dictionary 和 postings 服务于&quot;根据关键词找文档&quot;的检索需求；fast field 服务于&quot;根据文档取数值&quot;的聚合/排序需求；store 服务于&quot;根据文档取原文&quot;的展示需求。搜索引擎的性能正是来源于这种按访问模式拆分存储的设计。</p><h2 id="索引流程" tabindex="-1"><a class="header-anchor" href="#索引流程"><span>索引流程</span></a></h2><p><code>IndexWriter</code> 是用户向索引添加文档的入口。它的内部架构是一个多生产者-多消费者模型：用户线程通过一个有界 channel（容量 10,000 篇文档）将文档分发给若干工作线程（根据 CPU 核心数和内存预算自动决定，最多 8 个）。每个工作线程独立持有一个 <code>SegmentWriter</code>，在各自的内存空间中构建 segment 的中间表示。</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">    add_document()</span>
<span class="line">         │</span>
<span class="line">         ▼</span>
<span class="line">    IndexWriter ─── channel ──┬── Thread 1: SegmentWriter ──┐</span>
<span class="line">         │                    ├── Thread 2: SegmentWriter ──┤── flush ──▶ Segment 文件</span>
<span class="line">         │                    └── Thread N: SegmentWriter ──┘</span>
<span class="line">         │</span>
<span class="line">         ▼ commit()</span>
<span class="line">    meta.json (原子更新)</span>
<span class="line">         │</span>
<span class="line">         ▼ background</span>
<span class="line">    SegmentUpdater ──▶ MergePolicy ──▶ Merger</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>对于收到的每篇文档，<code>SegmentWriter</code> 做三件事：</p><ol><li>构建倒排索引：对文本字段运行分词器，将每个 token 及其位置信息写入内存中的 posting 结构（基于 <code>tantivy-stacker</code> 的 arena hashmap）</li><li>写入列式存储：将数值型 fast field 的值追加到列式 writer（基于 <code>tantivy-columnar</code>）</li><li>写入文档存储：将标记为 <code>STORED</code> 的字段序列化到 store writer 的压缩块中</li></ol><p>每个工作线程有一个内存上限（最少 15MB）。当内存使用接近上限时，线程将当前 segment flush 到磁盘，然后开始构建下一个新的 segment。注意 flush 不等于 commit——flush 产生的文件还不可被搜索到。</p><p>只有当用户显式调用 <code>commit()</code> 时，所有工作线程才会停止接收新文档、flush 各自正在构建的 segment，最后原子地更新 <code>meta.json</code> 将这些新 segment 注册为索引的一部分。此后新获取的 <code>Searcher</code> 就能看到这批新文档了。</p><p>随着 commit 次数增加，segment 数量会不断增长。过多的 segment 会拖慢搜索性能（每次搜索都需要遍历所有 segment），因此需要后台合并。<code>LogMergePolicy</code> 是默认的合并策略——它按文档数量的对数将 segment 分层，当同一层积累了 8 个及以上 segment 时触发合并。合并操作将多个小 segment 读取并合为一个大 segment，同时物理清除已标记删除的文档。合并在独立的线程池中执行，不阻塞索引和搜索。</p><h2 id="搜索流程" tabindex="-1"><a class="header-anchor" href="#搜索流程"><span>搜索流程</span></a></h2><p>搜索通过 <code>Searcher</code> 对象进行。<code>Searcher</code> 持有索引某一时刻的快照——一组 <code>SegmentReader</code> 的集合。无论后续发生了 commit、merge 还是文件回收，只要持有同一个 <code>Searcher</code>，搜索就始终在同一个不可变快照上进行。这是一种 MVCC（多版本并发控制）式的设计，读写之间完全无锁。</p><p><code>SegmentReader</code> 是对一个 segment 所有数据结构的只读访问入口，提供对所有存储结构的统一只读访问。由于底层数据几乎全部通过 mmap 映射，<code>SegmentReader</code> 的匿名内存占用极低——加载一个索引的速度本质上就是 mmap 这些文件的速度。</p><h3 id="查询系统抽象" tabindex="-1"><a class="header-anchor" href="#查询系统抽象"><span>查询系统抽象</span></a></h3><p>Tantivy 的查询系统分为三层抽象，每一层都有明确的职责：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">    QueryParser::parse_query(&quot;old sea&quot;)</span>
<span class="line">         │</span>
<span class="line">         ▼</span>
<span class="line">    Query                           定义匹配逻辑</span>
<span class="line">         │  .weight(searcher)</span>
<span class="line">         ▼</span>
<span class="line">    Weight                          绑定了全局统计信息的查询</span>
<span class="line">         │  .scorer(segment_reader) ← 对每个 segment 分别调用</span>
<span class="line">         ▼</span>
<span class="line">    Scorer                          在具体 segment 上遍历匹配文档</span>
<span class="line">         │</span>
<span class="line">         ▼</span>
<span class="line">    Collector                       收集/聚合结果</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>为什么不从 Query 直接产生结果，而要经过这三层？</p><ul><li><strong>Query</strong>：纯粹的逻辑描述——&quot;我要找包含 old 或 sea 的文档&quot;。它不关心索引有多少 segment，也不知道 old 这个词在整个索引中出现了多少次。</li><li><strong>Weight</strong>：在 Query 的基础上绑定了全局统计信息。BM25 评分需要每个 term 的逆文档频率（IDF），而 IDF 的计算需要汇总所有 segment 中该 term 的文档频率。Weight 正是承载这些全局统计的中间层——它被创建一次，随后可以为每个 segment 生成 Scorer。</li><li><strong>Scorer</strong>：绑定到具体 segment 的迭代器。持有该 segment 中对应 term 的 posting list，能够高效地遍历匹配的 DocId 并计算每篇文档的 BM25 分数。</li></ul><p>这个三层结构对应着三个粒度：整个查询级别、整个索引级别、单个 segment 级别。如果要实现一种新的 Query 类型，就需要对应地实现这三个 trait。</p><h3 id="collector-模式" tabindex="-1"><a class="header-anchor" href="#collector-模式"><span>Collector 模式</span></a></h3><p>搜索的最后一环是 Collector，它负责决定如何处理 Scorer 产生的 (DocId, Score) 对。Tantivy 为每个 segment 创建一个 <code>SegmentCollector</code>，各自独立收集结果，最后通过 <code>merge_fruits()</code> 将各 segment 的局部结果汇总为最终输出。</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">    Segment 0 ──▶ SegmentCollector 0 ──▶ top 10 of segment 0 ─┐</span>
<span class="line">    Segment 1 ──▶ SegmentCollector 1 ──▶ top 10 of segment 1 ─┤── merge ──▶ 全局 top 10</span>
<span class="line">    Segment 2 ──▶ SegmentCollector 2 ──▶ top 10 of segment 2 ─┘</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>内置的 Collector 包括：<code>TopDocs</code>（取评分最高的 K 篇文档）、<code>Count</code>（统计匹配文档总数）、<code>FacetCollector</code>（分面统计）等。因为 Collector 是一个 trait，用户也可以实现自定义的聚合逻辑。</p><h3 id="一次搜索的完整路径" tabindex="-1"><a class="header-anchor" href="#一次搜索的完整路径"><span>一次搜索的完整路径</span></a></h3><p>将上述概念串起来，当用户搜索 &quot;old sea&quot; 时，实际发生的事情是：</p><ol><li><code>QueryParser</code> 将字符串解析为一个 <code>BooleanQuery</code>（<code>TermQuery(&quot;old&quot;) OR TermQuery(&quot;sea&quot;)</code>）</li><li><code>BooleanQuery</code> 创建 <code>BooleanWeight</code>，过程中从所有 segment 汇总 &quot;old&quot; 和 &quot;sea&quot; 的文档频率以计算 IDF</li><li>对于每个 segment，<code>BooleanWeight</code> 创建 Scorer： <ul><li>在 term dictionary 中查找 &quot;old&quot; 和 &quot;sea&quot; → 得到各自的 <code>TermInfo</code>（posting list 的偏移范围）</li><li>根据 <code>TermInfo</code> 定位到 <code>.idx</code> 文件中对应的 posting list</li><li>两个 posting list 取并集，对每篇匹配文档计算 BM25 分数</li></ul></li><li><code>TopDocs</code> collector 在每个 segment 中只保留分数最高的 K 篇文档（内部通过带阈值的缓冲区实现剪枝）</li><li>各 segment 的 top-K 结果合并为全局 top-K</li><li>用户拿到 <code>DocAddress</code>（segment 序号 + DocId）后，可从 store 中取回文档原文用于展示</li></ol><h2 id="参考资料" tabindex="-1"><a class="header-anchor" href="#参考资料"><span>参考资料</span></a></h2><ul><li><a href="https://github.com/quickwit-oss/tantivy" target="_blank" rel="noopener noreferrer">Tantivy GitHub Repository</a></li><li><a href="https://github.com/quickwit-oss/tantivy/blob/main/ARCHITECTURE.md" target="_blank" rel="noopener noreferrer">Tantivy ARCHITECTURE.md</a></li><li><a href="https://fulmicoton.com/posts/behold-tantivy-part2/" target="_blank" rel="noopener noreferrer">Behold, Tantivy (Part 2) - Paul Masurel</a></li><li><a href="https://quickwit.io/docs" target="_blank" rel="noopener noreferrer">Quickwit Documentation</a></li></ul>`,83)])])}const c=n(t,[["render",l]]),r=JSON.parse('{"path":"/notes/Tantivy/Tantivy%20Architecture.html","title":"Tantivy - Architecture","lang":"en-US","frontmatter":{},"git":{},"filePathRelative":"notes/Tantivy/Tantivy Architecture.md"}');export{c as comp,r as data};
