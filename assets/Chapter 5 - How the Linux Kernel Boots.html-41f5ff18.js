import{_ as e,o as i,c as o,e as a}from"./app-25fa875f.js";const l={},d=a(`<h1 id="chapter-5-how-the-linux-kernel-boots" tabindex="-1"><a class="header-anchor" href="#chapter-5-how-the-linux-kernel-boots" aria-hidden="true">#</a> Chapter 5 - How the Linux Kernel Boots</h1><p>Created by : Mr Dk.</p><p>2019 / 06 / 25 0:14</p><p>@dorm, NUAA, Nanjing, Jiangsu, China</p><hr><p>内核如何启动？</p><ol><li>BIOS 或固件装载并运行 boot loader</li><li>boot loader 找到磁盘上的内核映像，将其装入内存并启动</li><li>内核初始化设备及其驱动</li><li>内核挂载根文件系统</li><li>内核启动 PID 为 1 的程序 <code>init</code> - 此时用户空间启动</li><li><code>init</code> 启动剩余的系统进程</li><li><code>init</code> 启动一个用于登录的进程</li></ol><h2 id="_5-1-startup-messages" tabindex="-1"><a class="header-anchor" href="#_5-1-startup-messages" aria-hidden="true">#</a> 5.1 Startup Messages</h2><p>传统的 Unix 系统会打印大量表示启动过程的信息，但现代系统已经尽可能隐藏这些信息了。如果想看，可以：</p><ul><li>查看内核日志文件 - <code>/var/log/kern.log</code></li><li>使用 <code>dmesg</code> 命令，但需要通过管道连接到 <code>less</code> 中，否则太多</li></ul><h2 id="_5-2-kernel-initialization-and-boot-options" tabindex="-1"><a class="header-anchor" href="#_5-2-kernel-initialization-and-boot-options" aria-hidden="true">#</a> 5.2 Kernel Initialization and Boot Options</h2><p>Linux 内核的初始化顺序：</p><ol><li>CPU 检查</li><li>存储器检查</li><li>发现设备总线</li><li>发现设备</li><li>内核辅助子系统启动</li><li>根文件系统挂载</li><li>用户空间启动</li></ol><h2 id="_5-3-kernel-parameters" tabindex="-1"><a class="header-anchor" href="#_5-3-kernel-parameters" aria-hidden="true">#</a> 5.3 Kernel Parameters</h2><p>在运行 Linux 内核时，boot loader 传递一系列文本参数 (kernel parameters)，告诉内核它应该如何启动。可以在 <code>/proc/cmdline</code> 中查看内核参数：</p><div class="language-console line-numbers-mode" data-ext="console"><pre class="language-console"><code>$ cat /proc/cmdline
BOOT_IMAGE=/boot/vmlinuz-4.15.0-51-generic root=UUID=af284c6e-d36f-410d-8c9e-950532e5ee89 ro console=tty1 console=ttyS0
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>one-word flags</li><li>key=value pairs</li></ul><p>其中，<code>root</code> 代表根文件系统所在位置：</p><ul><li>可以是一个设备文件（分区）</li><li>现在桌面系统中，UUID 更加流行</li></ul><p><code>ro</code> 命令内核以 read-only 的方式挂载根文件系统：</p><ul><li>确保 <code>fsck</code> 能够安全地检测根文件系统</li><li>检测完成之后，内核重新以 read-write 模式挂载根文件系统</li></ul><p>对于内核不认识的参数，内核会将其作为 <code>init</code> 函数的参数传递过去。</p><h2 id="_5-4-boot-loaders" tabindex="-1"><a class="header-anchor" href="#_5-4-boot-loaders" aria-hidden="true">#</a> 5.4 Boot Loaders</h2><p>在启动的过程中，由 boot loader 来启动内核。功能看起来很简单：将内核装载到内存中，将一些列 kernel parameter 传递给内核，并启动内核。但是 boot loader 必须能够回答以下问题：</p><ul><li>内核在哪里？</li><li>当内核启动时，应当将哪些参数传递给它</li></ul><p>显然，内核及其参数肯定位于根文件系统的某个位置。然而，此时内核还没有开始运行，因此无法通过遍历文件系统来寻找。此外，用于访问磁盘的设备驱动也无法使用 - <em>chicken or egg</em> problem。在 PC 上，boot loader 使用以下两种方式访问磁盘：</p><ul><li>Basic Input/Output System (BIOS)</li><li>Unified Extensible Firmware Interface (UEFI)</li></ul><p>几乎所有的磁盘硬件都有允许 BIOS 使用 <em>Linear Block Addressing (LBA)</em> 来访问存储的固件，这种访问方式性能很低，但允许磁盘的完全访问。Boot loader 通常是使用这种访问方式的唯一程序。内核使用其高性能的驱动程序访问磁盘。</p><p>有一些先进的 boot loader 能够读取分区表，并提供了内置以 read-only 的方式访问文件系统的支持。因此 boot loader 可以寻找并读取文件。</p><h3 id="_5-4-1-boot-loader-tasks" tabindex="-1"><a class="header-anchor" href="#_5-4-1-boot-loader-tasks" aria-hidden="true">#</a> 5.4.1 Boot Loader Tasks</h3><p>一个 Linux boot loader 的核心功能：</p><ul><li>在多个内核中选择</li><li>在一系列内核参数中切换</li><li>允许用户手动覆盖或编辑内核映像名和参数</li><li>提供启动其它操作系统的支持</li></ul><h3 id="_5-4-2-boot-loader-overview" tabindex="-1"><a class="header-anchor" href="#_5-4-2-boot-loader-overview" aria-hidden="true">#</a> 5.4.2 Boot Loader Overview</h3><ul><li>GRUB：Linux 系统的基本标配</li><li>LILO：最早期的 Linux boot loader 之一</li><li>SYSLINUX</li><li>LOADLIN：从 MS-DOS 启动内核</li><li>efilinux</li><li>coreboot</li><li>Linux Kernel EFISTUB - 内核插件，直接从 ESP 分区装载内核</li></ul><h2 id="_5-5-grub-introduction" tabindex="-1"><a class="header-anchor" href="#_5-5-grub-introduction" aria-hidden="true">#</a> 5.5 GRUB Introduction</h2><p>GRUB - <em>Grand Unified Boot Loader</em>。GRUB 最重要的能力是文件系统导航，使得选择内核映像和配置更加简单 (从 GRUB 的菜单就可以看出)。当 BIOS 或固件启动时，按住 shift 可以进入 GRUB 菜单；在菜单界面超时后，会自动启动。按下 <code>e</code> 按钮可以查看 boot loader 的配置命令。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>setparams &#39;Ubuntu, with Linux 3.2.0-31-generic-pae&#39;
recordfail
gfxmode $linux_gfx_mode
insmod gzio
insmod part_msdos
insmod ext2
set root=&#39;(hd0,msdos1)&#39;
search --no-floppy --fs-uuid --set=root 4898e145-b064-45hd-b7b4-7326b00273b7
linux /boot/vmlinuz-3.2.0-31-generic-pae root=UUID=489e145-b064-45bd-b7b4-7326b00273b7 ro quiet splash $vt_handoff
initrd /boot/initrd.img-3.2.0-31-generic-pae
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>内核映像为 <code>/boot/vmlinuz-3.2.0-31-generic-pae</code></li><li>根文件系统由 UUID 标识</li><li>内核参数包含 <code>ro</code>、<code>quiet</code>、<code>splash</code></li><li>初始化 RAM 文件系统为 <code>/boot/initrd.img-3.2.0-31-generic-pae</code></li></ul><p>GRUB 只用于启动内核，因此不依赖于内核。上述命令全部是 GRUB 的内部命令</p><ul><li>GRUB 有自己的内核</li><li>GRUB 有自己的 <code>insmod</code> 命令，用于动态载入 GRUB 模块</li><li>很多 GRUB 命令和 Unix shell 的命令类似，比如 <code>ls</code></li></ul><p>上述命令中，只有 linux 命令下的 root 表示根文件系统的位置。其它 root 是 GRUB 的 root，是 GRUB 搜索内核和 RAM 文件系统映像的位置。在上述命令中，<code>root</code> 首先被设定为 <code>hd0,msdos1</code>，并在之后指定了一个 UUID，GRUB 在分区上查找该 UUID。如果找到，则将 <code>root</code> 设定为该 UUID 所在分区，<code>linux</code> 和 <code>initrd</code> 命令表示从 GRUB root 所在分区中的 <code>/boot</code> 目录下载入映像。以上配置命令可以在 GRUB 中修改。</p><h3 id="_5-5-1-exploring-devices-and-partitions-with-the-grub-command-line" tabindex="-1"><a class="header-anchor" href="#_5-5-1-exploring-devices-and-partitions-with-the-grub-command-line" aria-hidden="true">#</a> 5.5.1 Exploring Devices and Partitions with the GRUB Command Line</h3><p>通过按下 <code>C</code> 可以进入 GRUB 命令行</p><h4 id="listing-devices" tabindex="-1"><a class="header-anchor" href="#listing-devices" aria-hidden="true">#</a> Listing Devices</h4><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>grub&gt; ls
(hd0)  (hd0,msdos1)  (hd0,msdos5)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>ls</code> 命令列出了 GRUB 识别的所有设备</li><li><code>msdos</code> 表示磁盘包含 MBR 分区表，因此可能会有 <code>gpt</code> 的前缀</li></ul><p>使用 <code>ls -l</code> 可以详细查看各个分区的信息：</p><ul><li>文件系统类型</li><li>大小、最后修改时间</li><li>UUID</li></ul><h4 id="file-navigation" tabindex="-1"><a class="header-anchor" href="#file-navigation" aria-hidden="true">#</a> File Navigation</h4><p>GRUB 的文件系统导航能力</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>grub&gt; echo $root
hd0,msdos1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>grub&gt; ls (hd0,msdos1)/
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>grub&gt; ls ($root)/
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>grub&gt; ls ($root)/boot
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>使用 <code>set</code> 命令可以查看所有的 GRUB 变量：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>grub&gt; set
?=0
color_highlight=black/white
color_normal=white/black
--snip--
prefix=(hd0,msdos1)/boot/grub
root=hd0,msdos1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>$prefix</code> 是 GRUB 寻找其配置文件和额外支持的路径。</p><h3 id="_5-5-2-grub-configuration" tabindex="-1"><a class="header-anchor" href="#_5-5-2-grub-configuration" aria-hidden="true">#</a> 5.5.2 GRUB Configuration</h3><p>GRUB 的配置目录包含一个核心配置文件 <code>grub.cfg</code> 和大量可装载的模块 <code>-.mod</code>。</p><h4 id="reviewing-grub-cfg" tabindex="-1"><a class="header-anchor" href="#reviewing-grub-cfg" aria-hidden="true">#</a> Reviewing Grub.cfg</h4><h4 id="generating-a-new-configuration-file" tabindex="-1"><a class="header-anchor" href="#generating-a-new-configuration-file" aria-hidden="true">#</a> Generating a New Configuration File</h4><h3 id="_5-5-3-grub-installation" tabindex="-1"><a class="header-anchor" href="#_5-5-3-grub-installation" aria-hidden="true">#</a> 5.5.3 GRUB Installation</h3><h4 id="installing-grub-on-your-system" tabindex="-1"><a class="header-anchor" href="#installing-grub-on-your-system" aria-hidden="true">#</a> Installing GRUB on Your System</h4><p>安装 boot loader 需要用户或安装器决定：</p><ul><li>GRUB 的目标路径，默认为 <code>/boot/grub</code>，但其它操作系统不同</li><li>GRUB 安装的目标设备</li><li>对于 UEFI 启动，还需要 UEFI boot 分区的挂载点</li></ul><p>GRUB 是一个模块化的系统，需要读取文件系统下的 GRUB 路径。所以在 Linux 上，只需要将 GRUB 和 ext2.mod 预装载版本放置在磁盘的启动区，其余部分放置在 GRUB 的目录下即可。</p><h2 id="_5-6-uefi-secure-boot-problems" tabindex="-1"><a class="header-anchor" href="#_5-6-uefi-secure-boot-problems" aria-hidden="true">#</a> 5.6 UEFI Secure Boot Problems</h2><p>UEFI 的安全机制要求 boot loader 必须被可信方签名后才可运行，无法安装未被签名的 boot loader。</p><h2 id="_5-7-chainloading-other-operating-system" tabindex="-1"><a class="header-anchor" href="#_5-7-chainloading-other-operating-system" aria-hidden="true">#</a> 5.7 Chainloading Other Operating System</h2><p>UEFI 使得装载其它操作系统变得简单，因为可以在 EFI 分区中安装多个 boot loader。通过 chainloading，可以使 GRUB 装载并运行一个指定磁盘分区上的另一个 boot loader。</p><h2 id="_5-8-boot-loader-details" tabindex="-1"><a class="header-anchor" href="#_5-8-boot-loader-details" aria-hidden="true">#</a> 5.8 Boot Loader Details</h2><p>两种主要的启动模式：</p><ul><li>MBR</li><li>UEFI</li></ul><h3 id="_5-8-1-mbr-boot" tabindex="-1"><a class="header-anchor" href="#_5-8-1-mbr-boot" aria-hidden="true">#</a> 5.8.1 MBR Boot</h3><p>Master Boot Record (MBR) 除了包含分区信息以外，还包括一个 <code>441</code> Bytes 的区域。用于 BIOS 在 <em>Power-On Self-Test (POST)</em> 之后装载并执行。但由于这个区域对于 boot loader 来说太小，需要一个额外的空间 - 即所谓的 <em>multi-stage boot loader</em>。此时，MBR 的初始空间只用于装载 boot loader 的剩余代码，剩余部分的 boot loader 代码通常位于 MBR 和磁盘第一个分区之间。</p><h3 id="_5-8-2-uefi-boot" tabindex="-1"><a class="header-anchor" href="#_5-8-2-uefi-boot" aria-hidden="true">#</a> 5.8.2 UEFI Boot</h3><p>PC 制造商和软件公司意识到传统的 BIOS 有诸多局限，因此开发了替代品 - <em>Extensible Firmware Interface (EFI)</em>，目前的标准是 <em>Unified EFI (UEFI)</em>：</p><ul><li>包括一个内置的 shell</li><li>和读取分区表，导航文件系统的能力</li></ul><p>GPT 分区模式是 UEFI 标准的一部分。总是会有一个特殊的文件系统 - <em>EFI System Partition (ESP)</em>。</p><ul><li>包含一个名为 <code>efi</code> 的目录</li><li>每个 boot loader 有其自身的标识和对应的子目录</li><li><code>.efi</code> 的 boot loader 文件分别存放在每个子目录中</li></ul><p>BIOS 和 UEFI 的 boot loader 版本是不同的。</p><h3 id="_5-8-3-how-grub-works" tabindex="-1"><a class="header-anchor" href="#_5-8-3-how-grub-works" aria-hidden="true">#</a> 5.8.3 How GRUB Works</h3><ol><li>BIOS/固件初始化硬件，搜索带有 boot code 的存储设备</li><li>BIOS/固件载入并执行 boot code - GRUB begins</li><li>GRUB core 装载</li><li>GRUB core 初始化，现在 GRUB 可以开始访问磁盘和文件系统</li><li>GRUB 识别其 boot 分区，加载配置文件</li><li>GRUB 给用户提供修改配置的机会</li><li>在用户选择或超时后，GRUB 执行当前配置</li><li>GRUB 在 boot 分区中加载其它代码 (modules)</li><li>GRUB 执行 boot 命令，装载并执行内核</li></ol><p>问题：<strong>Where is the GRUB core?</strong> 除了 ESP 以外，BIOS 读取 MBR 的 <code>512</code> 字节，即 GRUB 开始的地方，不包含 GRUB core，但包含 GRUB 的起始位置，和从该位置加载的代码。如果使用 ESP，则固件可以直接寻找到 GRUB core 的文件并执行。</p>`,84),r=[d];function n(t,s){return i(),o("div",null,r)}const u=e(l,[["render",n],["__file","Chapter 5 - How the Linux Kernel Boots.html.vue"]]);export{u as default};
