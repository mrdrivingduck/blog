language: node_js
node_js:
  - "node"

cache: npm

script: npm run build

deploy:
  provider: pages
  cleanup: false
  token: $GITHUB_TOKEN
  keep_history: true
  target_branch: gh-pages
  local_dir: ./dist/
  on:
    branch: master